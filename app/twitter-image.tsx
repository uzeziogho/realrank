// Twitter card reuses the same generated image as Open Graph.
// (runtime is intentionally not re-exported — Next uses its default for this file.)
export { default, alt, size, contentType } from "./opengraph-image";
